<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/vectorlogo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme_activityMain"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ListActivity"
            android:theme="@style/ListPersonActivity"
            android:label="@string/voters"
            android:parentActivityName=".HomeScreen"
            android:exported="false" />
        <activity
            android:name=".SearchActivity"
            android:theme="@style/Theme_searchActivity"
            android:label="@string/search_voter"
            android:exported="false" />
        <activity
            android:name=".UpdateActivity"
            android:theme="@style/Theme_updateActivity"
            android:label="update voter"
            android:exported="false" />
        <activity
            android:name=".SearchResultActivity"
            android:exported="false" />
        <activity
            android:name=".HomeScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/create_voter"
            android:theme="@style/Theme_MainActivity"/>
    </application>

</manifest>